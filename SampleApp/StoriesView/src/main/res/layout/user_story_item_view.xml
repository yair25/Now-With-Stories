<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="4dp">

    <RelativeLayout
        android:id="@+id/storyFrame"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/ic_circle_gradient">

        <ImageView
            android:id="@+id/storyImgView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="4dp"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_person_black_24dp"
            android:transitionName="sharedProfilePicTransition"
            android:contentDescription="" />
    </RelativeLayout>

    <TextView
        android:id="@+id/storyTextView"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="@string/story_item_desc"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:maxLines="1"
        android:transitionName="sharedUserNameTransition" />
</LinearLayout>